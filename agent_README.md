# Agent 架构设计说明

## 概述

本文档描述 GenderShield 对话 Agent 的整体架构设计思路。当前处于**早期设计阶段**，架构会随语料收集进度与技术调研结果持续调整。

设计的核心原则是：**安全优先（Safety by Design）**——面对家暴/性别暴力受害者这一高度脆弱的用户群体，Agent 的任何一次错误回应，都可能造成二次伤害。因此架构设计上会优先考虑"可控性"与"可解释性"，而非单纯追求对话的流畅度或"智能感"。

---

## 整体流程（Pipeline）

```
用户输入
   │
   ▼
① 安全与危机检测层（Safety & Crisis Detection）
   │
   ▼
② 意图识别（Intent Recognition）
   │
   ▼
③ 情感/情绪识别（Emotion Recognition）
   │
   ▼
④ 对话状态管理（Dialogue State Management）
   │
   ▼
⑤ 回应生成（Response Generation）
   │
   ▼
⑥ 资源推荐（Resource Recommendation，如需要）
   │
   ▼
输出给用户
```

---

## 各模块说明

### ① 安全与危机检测层
- **优先级最高，独立于其他模块运行**
- 检测用户输入中是否包含自杀倾向、即时人身危险、儿童相关侵害等高风险信号
- 一旦触发，**跳过常规对话流程**，直接返回预设的紧急求助信息（如法国家暴热线3919、当地警方等），并明确建议用户联系专业人士
- 该模块**不追求"智能"，而追求"可靠"**：宁可误报，也不能漏报

### ② 意图识别（Intent Recognition）
- 识别用户当前诉求类型，例如：
  - 寻求情感倾诉/陪伴
  - 咨询法律信息（如如何报案、申请保护令）
  - 咨询心理援助资源
  - 单纯想了解"这是不是家暴/是不是正常的"（认知澄清类需求）
- 初期计划：基于规则 + 关键词匹配的轻量方案；后续视语料规模再评估是否引入微调后的分类模型

### ③ 情感/情绪识别（Emotion Recognition）
- 识别用户当前情绪状态（如恐惧、愤怒、麻木、羞耻感等）
- 这一部分会尝试引入社会语言学中的**话语分析方法**，而不只是套用通用情感分析工具——因为通用情感分析模型往往是基于英语语境训练的，对中文语境下性别暴力话语的隐晦表达（如自责式表达、淡化式表达）识别效果有限
- 计划结合小规模标注语料，观察受害者常见的语言模式（hedging、self-blame markers等）

### ④ 对话状态管理（Dialogue State Management）
- 跟踪对话历史，避免重复提问或答非所问
- 管理多轮对话中的上下文（例如用户已经提到过的具体情境，不需要重复询问）

### ⑤ 回应生成（Response Generation）
- **初期不采用完全自由生成式回复**，而是"生成式框架 + 人工审核过的回应模板"相结合
- 原因：完全依赖大语言模型自由生成，存在给出不当建议、评判性语言、或过度承诺（如"我理解你的一切"）的风险，这在该场景下是不可接受的
- 语言风格要求：**非评判性（non-jugeant）、不指导性（non-directif）、赋权性（valorisant l'autonomie de la personne）**

### ⑥ 资源推荐
- 根据用户所在地区/语言，推荐对应的官方援助资源（热线、机构、法律援助点等）
- 资源库需要**定期人工核实更新**，避免推荐失效或错误的联系方式

---

## 技术选型（待确认）

| 模块 | 候选方案 |
|---|---|
| 意图识别 | 规则匹配（早期）→ 小样本微调分类器（后期） |
| 情感/话语分析 | 自建标注体系 + spaCy / 定制规则 |
| 对话管理 | Rasa 或自建有限状态机（FSM） |
| 回应生成 | 模板库为主，LLM API 为辅（需严格约束prompt与输出范围） |

---

## 尚未解决的开放问题

- [ ] 如何在保证隐私的前提下收集/构建适用于该场景的对话语料？
- [ ] 危机检测的准确率与召回率如何评估？是否需要引入人工审核环节？
- [ ] 是否需要区分中文语境与法语语境下的不同话语模式（涉及跨语言迁移问题）？
- [ ] Agent 的"人设"边界如何设定？如何避免用户对 Agent 产生过度情感依赖？

---

## 参考方向

本模块的设计会持续参考社会语言学中关于**话语分析（Analyse du Discours）**与**互动社会学理论（Théorie de l'interaction sociale）**的相关研究方法，尝试将其转化为可用于对话系统设计的具体规则，而不是单纯套用现成的NLP工具链。
